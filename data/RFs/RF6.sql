-- Create

INSERT INTO PLANESDECONSUMO (ID, NOMBRE, DESCRIPCION, DESCUENTO)
VALUES (20, 'Familiar', 'Costo reducido para menores de edad', 0.2);

INSERT INTO PLANESDECONSUMO (ID, NOMBRE, DESCRIPCION, DESCUENTO)
VALUES (30, 'Vacacional', 'Servicios menos costosos', 0.3);

-- Read

SELECT *
FROM PLANESDECONSUMO
WHERE NOMBRE = 'Todo Incluido';

SELECT *
FROM PLANESDECONSUMO
WHERE DESCUENTO > 0.4;

-- Update

UPDATE PLANESDECONSUMO
SET DESCUENTO = DESCUENTO - 0.05
WHERE DESCUENTO > 0;

-- Delete

DELETE
FROM PLANESDECONSUMO
WHERE NOMBRE = 'Todo Incluido';

DELETE
FROM PLANESDECONSUMO
WHERE DESCUENTO >= 0.5;