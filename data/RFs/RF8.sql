-- Create

INSERT INTO RESERVASSERVICIO (ID, FECHA, HORAINICIO, HORAFIN, RESERVAS_ID, SERVICIOS_ID)
VALUES (20, TO_DATE('2023-01-01', 'YYYY-MM-DD'), TO_DATE('11:00', 'HH24:MI'), TO_DATE('12:00', 'HH24:MI'), 2, 4);

INSERT INTO RESERVASSERVICIO (ID, FECHA, HORAINICIO, HORAFIN, RESERVAS_ID, SERVICIOS_ID)
VALUES (30, TO_DATE('2025-02-15', 'YYYY-MM-DD'), TO_DATE('16:30', 'HH24:MI'), TO_DATE('17:30', 'HH24:MI'), 3, 5);

-- Read

SELECT *
FROM RESERVASSERVICIO
WHERE SERVICIOS_ID = 1;

SELECT *
FROM RESERVASSERVICIO
WHERE FECHAINICIO >= TO_DATE('2020-01-01', 'YYYY-MM-DD');

-- Update

UPDATE RESERVASSERVICIO
SET HORAINICIO = HORAINICIO - 2, HORAFIN = HORAFIN + 2
WHERE RESERVAS_ID = 1;

-- Delete

DELETE
FROM RESERVASSERVICIO
WHERE RESERVAS_ID = 1;

DELETE
FROM RESERVASSERVICIO
WHERE SERVICIOS_ID = 1;