-- Create

INSERT INTO RESERVAS (ID, FECHAINICIO, FECHAFIN, NUMNOCHES, NUMADULTOS, NUMMENORES, USUARIOS_ID, PLANESDECONSUMO_ID)
VALUES (20, TO_DATE('2040-01-01', 'YYYY-MM-DD'), TO_DATE('2040-12-31', 'YYYY-MM-DD'), 365, 2, 3, 1, 1);

INSERT INTO RESERVAS (ID, FECHAINICIO, FECHAFIN, NUMNOCHES, NUMADULTOS, NUMMENORES, USUARIOS_ID, PLANESDECONSUMO_ID)
VALUES (30, TO_DATE('2050-05-15', 'YYYY-MM-DD'), TO_DATE('2050-05-20', 'YYYY-MM-DD'), 5, 2, 1, 2, 2);

-- Read

SELECT *
FROM RESERVAS
WHERE USUARIOS_ID = 1;

SELECT *
FROM RESERVAS
WHERE NUMNOCHES <= 10;

-- Update

UPDATE RESERVAS
SET NUMNOCHES = NUMNOCHES + 5
WHERE USUARIOS_ID = 1;

UPDATE RESERVAS
SET NUMNOCHES = NUMNOCHES + 1, FECHAFIN = FECHAFIN + 1;

-- Delete

DELETE
FROM RESERVAS
WHERE USUARIOS_ID = 1;

DELETE
FROM RESERVAS
WHERE PLANESDECONSUMO_ID IS NULL;